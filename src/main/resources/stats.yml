expressions:
  left-click:
    damage: "baseValue * (globalBonusValue + inputBonusValue) * (classCriticalDamage / classCriticalChance)"
    attack-speed: "baseValue * (1 + inputBonusValue)"
    shield: "baseValue + inputBonusValue"

regeneration-per-second:
  shield: 1